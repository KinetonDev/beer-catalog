import {makeStyles} from "@mui/styles";

const useStyles = makeStyles({
    beerCharacteristics: {
        display: "flex",
        marginBottom: "20px"
    },
    beerProperties: {
        width: "30%",
        marginRight: "12%"
    },
    beerFoodPairing: {
        width: "20%"
    },
    title: {
        marginBottom: "10px"
    }
});

export default useStyles;