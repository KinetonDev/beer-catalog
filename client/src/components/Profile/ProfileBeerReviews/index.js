import React from 'react';
import PropTypes from 'prop-types';

const ProfileBeerReviews = (props) => {
    return (
        <div>
            Reviews
        </div>
    );
};

ProfileBeerReviews.propTypes = {};

export default ProfileBeerReviews;