import {makeStyles} from "@mui/styles";

const useStyle = makeStyles({
    loader: {
        position: "fixed",
        left: "50%",
        top: "50%",
        zIndex: "1000",
    }
});

export default useStyle;